#ifndef COMMANDS_H
#define COMMANDS_H

enum class GameCommand {
    USE_ABILITY,
    DEAL_DAMAGE,
    SAVE_GAME,
    LOAD_GAME,
    EXIT,
    ATTACK,
    UNKNOWN
};

#endif // COMMANDS_H
